require 'spec_helper'

describe ProductBundler do

  before(:each) do
    @product_bundler = ProductBundler.new
    # ProductBundler.stub(:product_range).and_return({ products: [ product_roses_default ] })
  end

  describe 'initialize' do

  end
  
end
