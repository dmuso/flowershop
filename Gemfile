source 'https://rubygems.org'

# not sure whether environments will be used yet

group :test do
  # for running the tests
  gem 'rspec', "~> 2.14.1"
end

group :development do
  # optional, but handy to use while developing
  gem 'guard', "~> 2.5.1"
  gem 'guard-rspec', "~> 4.2.8"
end
